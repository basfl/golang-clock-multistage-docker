# golang clock app using multistage docker
###  building Dockerfile using command:
    * docker image build -t {tag name} . 
### building Dockerfile using compose::
    * docker-compose up
### Getting image from dockehub:
    * docker pull basfl/golang-clock:first


